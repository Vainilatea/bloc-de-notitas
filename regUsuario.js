const express = require('express')
const app = express();
const conexion = require ("../config/conexion");

Router.post("/regUsuario", function(req, res)){

    let ide = req.body.campoIde;
    let ide = req.body.campoIde;
    let ide = req.body.campoIde;
    let ide = req.body.campoIde;

}